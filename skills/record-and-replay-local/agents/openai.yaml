interface:
  display_name: "FlowOnce"
  short_description: "Teach AI a Mac workflow once, then reuse it"
  default_prompt: "Use $record-and-replay-local to check replay readiness, learn and summarize my Mac workflow, then prove it works with changed safe inputs."
policy:
  allow_implicit_invocation: true
